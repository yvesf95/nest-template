# To view what the actual values will be used by the docker compose file, run:
# $ docker compose config

version: '3.9'

services:
  postgres:
    container_name: postgres
    image: postgres:16
    ports:
      - ${DB_PORT}:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    restart: always

  pgadmin:
    container_name: pgadmin
    image: dpage/pgadmin4:latest
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_EMAIL:-admin@email.com}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PW:-Test1234}
    ports:
      - "5050:80"
    restart: always

volumes:
  pgdata:
